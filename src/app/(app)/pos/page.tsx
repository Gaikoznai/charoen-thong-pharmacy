'use client';

export default function POSPage() {
  return (
    <div style={{ display: 'flex', flexDirection: 'column', flex: 1, overflow: 'hidden', background: '#fff' }}>
      <div style={{ textAlign: 'center', marginTop: '40px', color: '#888', fontSize: '16px' }}>
        กำลังพัฒนา...
      </div>
    </div>
  );
}
